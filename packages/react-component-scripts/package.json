{
  "name": "node-package-example",
  "version": "1.0.0",
  "files": ["lib"],
  "devDependencies": {
    "@tradie/node-package-template": "^1.1.0-alpha.b2a4b8e1"
  },
  "scripts": {
    "clean": "tradie clean",
    "lint": "tradie lint",
    "build": "tradie build",
    "watch": "tradie build --watch",
    "test": "tradie test",
    "prepublish": "tradie clean && tradie lint && tradie test && tradie build"
  }
}
