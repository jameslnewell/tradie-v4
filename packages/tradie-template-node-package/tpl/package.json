{
  "name": "tradie-template-node-package-example",
  "version": "1.0.0",
  "devDependencies": {
    "tradie-template-node-package": "^<%= version %>"
  },
  "scripts": {
    "clean": "tradie clean",
    "lint": "tradie lint",
    "build": "tradie build",
    "watch": "tradie build --watch",
    "test": "tradie test"
  }
}