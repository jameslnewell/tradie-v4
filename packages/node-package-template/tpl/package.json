{
  "name": "@tradie/node-package-template-example",
  "version": "1.0.0",
  "devDependencies": {
    "@tradie/node-package-template": "^<%= version %>"
  },
  "scripts": {
    "clean": "tradie clean",
    "lint": "tradie lint",
    "build": "tradie build",
    "watch": "tradie build --watch",
    "test": "tradie test"
  }
}