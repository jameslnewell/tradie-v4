{
  "name": "@tradie/node-package-example",
  "version": "1.1.3",
  "private": true,
  "main": "lib/index.js",
  "files": [
    "lib",
    ".flowconfig"
  ],
  "engines": {
    "node": "8"
  },
  "devDependencies": {
    "@tradie/node-package-scripts": "^1.1.3"
  },
  "scripts": {
    "clean": "tradie clean",
    "lint": "tradie lint",
    "build": "tradie build",
    "watch": "tradie build --watch",
    "test": "tradie test",
    "example": "tradie example",
    "prepublishOnly": "tradie clean && tradie lint && tradie test && tradie build"
  }
}
